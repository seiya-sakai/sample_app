<!DOCTYPE html>
<html lang="ja" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @users.each do |user| %>
     <div class="user-name">
      <%=link_to(user.name,"/users/#{user.id}") %>
     </div>
    <% end %>
  </body>
</html>
